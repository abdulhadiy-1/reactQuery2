import Header from "../modules/Header"
import PageRoutes from "../routes"

const Layout = () => {
  return (
    <div>
        <Header />
        <PageRoutes />
    </div>
  )
}

export default Layout